package ar.com.patterns.structural.bridge;

/** Implementor
 * It defines the interface for implementation classes. This interface
 * does not need to correspond directly to the abstraction interface and can be very different.
 */
public interface Workshop {

    void work();
}
